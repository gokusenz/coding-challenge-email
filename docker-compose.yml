version: '2'

services:

# ### Golang Container #########################################

    email-service:
        # image: test
        # restart: always
        build:
            context: .
        ports:
            - "8080:8080"
            # - "443:443"
