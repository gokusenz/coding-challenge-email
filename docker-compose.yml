version: '2'

services:

# ### Golang Container #########################################

    email-service:
        # image: test
        # restart: always
        build:
            context: .
        ports:
            - "80:80"
            # - "443:443"
